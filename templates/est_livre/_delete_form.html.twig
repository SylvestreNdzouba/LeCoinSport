<form method="post" action="{{ path('app_est_livre_delete', {'id': est_livre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ est_livre.id) }}">
    <button class="btn">Delete</button>
</form>
