{% extends 'base.html.twig' %}

{% block title %}EstLivre index{% endblock %}

{% block body %}
    <h1>EstLivre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_commande</th>
                <th>Id_livraison</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for est_livre in est_livres %}
            <tr>
                <td>{{ est_livre.id }}</td>
                <td>{{ est_livre.idCommande }}</td>
                <td>{{ est_livre.idLivraison }}</td>
                <td>
                    <a href="{{ path('app_est_livre_show', {'id': est_livre.id}) }}">show</a>
                    <a href="{{ path('app_est_livre_edit', {'id': est_livre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_est_livre_new') }}">Create new</a>
{% endblock %}
