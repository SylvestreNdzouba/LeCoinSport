{% extends 'base.html.twig' %}

{% block title %}Acheteur index{% endblock %}

{% block body %}
    <h1>Acheteur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Tel</th>
                <th>Email</th>
                <th>Id_ville</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for acheteur in acheteurs %}
            <tr>
                <td>{{ acheteur.id }}</td>
                <td>{{ acheteur.nom }}</td>
                <td>{{ acheteur.prenom }}</td>
                <td>{{ acheteur.tel }}</td>
                <td>{{ acheteur.email }}</td>
                <td>{{ acheteur.idVille }}</td>
                <td>
                    <a href="{{ path('app_acheteur_show', {'id': acheteur.id}) }}">show</a>
                    <a href="{{ path('app_acheteur_edit', {'id': acheteur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_acheteur_new') }}">Create new</a>
{% endblock %}
