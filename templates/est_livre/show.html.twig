{% extends 'base.html.twig' %}

{% block title %}EstLivre{% endblock %}

{% block body %}
    <h1>EstLivre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ est_livre.id }}</td>
            </tr>
            <tr>
                <th>Id_commande</th>
                <td>{{ est_livre.idCommande }}</td>
            </tr>
            <tr>
                <th>Id_livraison</th>
                <td>{{ est_livre.idLivraison }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_est_livre_index') }}">back to list</a>

    <a href="{{ path('app_est_livre_edit', {'id': est_livre.id}) }}">edit</a>

    {{ include('est_livre/_delete_form.html.twig') }}
{% endblock %}
